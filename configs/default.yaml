project:
  name: qr-haven
  environment: local

paths:
  data: data
  models: models
  experiments: experiments

data_sources:
  prices:
    source_type: csv
    path: data/raw/prices.csv
    table: prices

research:
  default_frequency: daily
  base_currency: USD

market_terminal:
  enabled: false
  plugin_namespace: qr_haven
